Use pip install -r requirements.txt for python 2.x
pip3 install -r requirements.txt for python 3.x even though I don know of the program ill run

Install virtualenv with pip to create a virtualenv for python.
tensorboard --logdir=path/to/log/file (in this case the logpath is graph) and then open the site to 
see the model used for the neural net.

Create a virtualenv with virtualenv and then run requierements.txt

this is a version of the code of Ivan Seidel even thought there are many things implemented differently but the sensors and the scanner utility is the same.
He also has a great video.
idea and I looked at his code Ivan Seidel[link]
